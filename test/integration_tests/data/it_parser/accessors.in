def func printAll(fmt, items) {
    io.print(fmt)
    for (def var i = 0, i < items.length, i++) {
        io.print(items[i])
    }
}

def var cell = grid[row].cells[col].value
def var first = items[0]
def var total = math.sum(items)
